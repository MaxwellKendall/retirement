import createHistory from 'history/createBrowserHistory';

const configureHistory = () => createHistory({});
// const configureHistory = () => createHistory();

export default configureHistory;

//TODO: THIS SHOULD RETURN THE HISTORY OBJECT 
